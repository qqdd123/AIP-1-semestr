В данной строке вставить после каждого символа 'a' символ 'b'.


#include <iostream>
#include <regex>
#include <string>

using namespace std;
int main() {

	string line;
	cin >> line;
	line = regex_replace(line, regex("a"), "ab");
	cout << line << endl;
	cin.get();
}
